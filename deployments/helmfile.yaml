environments:
  dev:
    values:
      - environments/dev.yaml
      - secrets/dev.yaml

releases:
  - name: hello-app
    namespace: default
    chart: /charts/hello-app
    version: 0.1.0
    values:
      - /charts/hello-app/values.yaml
