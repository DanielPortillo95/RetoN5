environments:
  dev:
    values:
      - environments/dev.yaml
      - secrets/dev.yaml

releases:
  - name: hello-app
    namespace: default
    chart: charts/hello-app
    values:
      - charts/hello-app/values.yaml
