environments:
  dev:
    secrets:
      - environments/dev.yaml
      - secrets://secrets/dev.yaml

releases:
  - name: hello-app
    namespace: default
    chart: ./hello-app
    values:
      - hello-app/values.yaml

